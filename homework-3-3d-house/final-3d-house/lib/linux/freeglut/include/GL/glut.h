#ifndef ECE433_BUNDLED_GLUT_COMPAT_H
#define ECE433_BUNDLED_GLUT_COMPAT_H

/* Compatibility wrapper for projects that include <GL/glut.h> while the
 * bundled Linux freeglut headers provide <GL/freeglut.h> only.
 */
#include "freeglut.h"

#endif /* ECE433_BUNDLED_GLUT_COMPAT_H */
